# Database path (SQLite)
DATABASE_PATH=/tmp/nomenclator.db

# Anthropic API key for Claude Haiku
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Monthly spend cap in USD
MONTHLY_SPEND_CAP_USD=20.0

# Auth password hash (generate with: python -c "from argon2 import PasswordHasher; print(PasswordHasher().hash('your_password'))")
AUTH_PASSWORD_HASH=
kkk:q!:q
